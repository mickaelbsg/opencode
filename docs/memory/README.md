# Memória do Projeto

Copie os arquivos de `templates/` para `docs/memory/` em um projeto para criar a memória específica dele.

Memórias globais permanecem em `~/.config/opencode/memory/` e não devem ser commitadas junto com um projeto.
